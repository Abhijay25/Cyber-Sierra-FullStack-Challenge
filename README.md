# Cyber Sierra FullStack Challenge

AI-powered CSV/Excel analyser.

## Setup

Copy `.env.example` to `.env` in `backend/` and add your OpenAI key.

## Run

make dev   # starts FastAPI on :8000 and Next.js on :3000
